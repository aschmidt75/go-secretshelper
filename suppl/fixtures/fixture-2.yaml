vaults:
  - name: kv
    type: age-file
    spec:
      path: ./suppl/fixtures/test-agefile
      identity: ./suppl/fixtures/test-identity

secrets:
  - type: secret
    vault: kv
    name: test

sinks:
  - type: file
    var: test
    spec:
      path: ./go-secrethelper-test.dat
      mode: 400

